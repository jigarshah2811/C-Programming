/*
 * REPLACE SPACE with %20 in URL
 * */

#include <stdio.h>
#include <stdlib.h>

void replace_space(char url[]){

	puts(url);

	// count space
	// allocate new memory (space * 3)
	// replace from end


}

int main(){

	char url[] = "sp sp sp";

	replace_space(url);

	return 0;
}
